# Copy to `mobile/android/keystore.properties` (this file should NOT be committed)
# Paths are resolved from `mobile/android/`

storeFile=../keys/android/release.keystore
storePassword=CHANGE_ME
keyAlias=toyutoyu
keyPassword=CHANGE_ME
